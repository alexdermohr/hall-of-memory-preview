# Hall of Memory — customer preview

Generated static customer-preview mirror. Not the canonical source repository.

Primary comparison route: `/demo/rahmen/` (variants 1–6). The overview shows only the frame choices. On each detail page, the image size can be adjusted continuously between the calibrated inner and outer decorative frame lines.

V6 uses the replacement floral full-frame source supplied on 14.08.2026. V1–V5 use the static convex gold-metal profile model with one upper-left light source; V1/V2 are stronger and V3–V5 intentionally restrained. No pointer tilt or parallax is used.

Search indexing is disabled in the generated HTML via `noindex,nofollow`.
