# Hall of Memory — customer preview

Generated static customer-preview mirror. Not the canonical source repository and not a production deployment.

Primary customer route: `/demo/`. It presents the event-first customer journey with customer-facing copy, real `Mehr erfahren` disclosures, a direct inquiry path, package consultation state, gallery preview and protected-event-gallery explanation.

Frame comparison route: `/demo/rahmen/` (variants 1–6). `/demo/` itself does not silently adopt an unselected comparison frame.

All internal links and public assets are base-path aware for `/hall-of-memory-preview/`; the publication gate rejects escaping root references. Search indexing remains disabled via `noindex,nofollow`.
